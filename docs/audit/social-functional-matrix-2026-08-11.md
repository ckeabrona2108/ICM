# Social functional matrix — final audit

Date: 2026-08-11

Statuses follow the requested final-report vocabulary: PASS, FIXED, PARTIAL, BLOCKED. Static/unit evidence alone is never treated as full runtime proof. `FIXED` is used only for the narrow route-contract defect; write/refresh or broader feature claims remain PARTIAL when the A/B/C environment was unavailable.

| FEATURE | EXPECTED | ACTUAL | STATUS | ROOT CAUSE / LIMIT | FILES | TEST COVERAGE | FIX STATUS |
|---|---|---|---|---|---|---|---|
| Routes / contract sync | `/feed` and dashboard Community consume one social contract | Both entry points load `getPublicFeedPayload` and render `PublicFeedPage`; `/community` redirects to `/feed` | FIXED | Legacy dashboard route bypassed the canonical contract; cross-entry mutation/cache sync is a separate unverified concern | `src/app/feed/page.tsx`; `src/app/(dashboard)/dashboard/community/page.tsx` | Render-only guest browser smoke; route inspection | Narrow routing/contract defect FIXED |
| Posts | Create/delete/persist in feed, Community, profile | Canonical create/delete and owner media cleanup are implemented | PARTIAL | No safe A/B write/refresh runtime; historical detail/profile reads still use bounded snapshots | post routes; `artist-social-service.ts`; feed components | Service and media lifecycle regressions | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Media | Preserve image(s), video, audio, supported mixed media | Structured media preserves order; max 8, max 1 video, max 1 audio; active renderers consume it | PARTIAL | Writable browser/S3 fixtures unavailable | `artist-community-dashboard.tsx`; profile/feed renderers; `social-post-media.ts` | Media normalization/post validation tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Media lifecycle | Clean discarded/deleted owned objects; reject foreign deletion | Explicit discard/post/comment delete performs owner/reference-checked cleanup | PARTIAL | Crash, lost response, or abandoned upload still needs an orphan sweeper | social media API route; composer; post/comment delete routes | Ownership and deletion-key tests | Explicit lifecycle IMPLEMENTED; crash cleanup remains |
| Release attachment | Attach existing release without mutating it | Canonical relation and rendering path are present | PARTIAL | Publish → refresh → open could not run on safe writable fixtures | post route/service; feed/profile components | Post contract tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Reactions | Toggle/switch one reaction with exact counters and rollback | Typed reactions are active; self-notification is suppressed | PARTIAL | Concurrent first-click/double-click behavior lacks isolated DB runtime proof | post/release reaction routes; social/scene services | Toggle/switch and self-notification tests | IMPLEMENTED — CONCURRENCY RUNTIME NEEDED |
| Comments | Create/delete with exact visible counters | Deleted leaves are hidden; required deleted ancestors remain; totals exclude soft-deleted rows | PARTIAL | A/B ownership, hard refresh, and request-failure UI not runtime-tested | comment routes; `artist-social-service.ts`; feed helpers | Tree/count/validation tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Replies | Persist parent, nest, notify, count | Canonical reply contract is mounted; deleted parents are rejected | PARTIAL | Cross-user notification/refresh unavailable | comment routes/service; feed detail/list | Thread and deleted-parent tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Comment pagination | Page roots in DB, then hydrate selected replies | Post/release services select stable root pages in Prisma and load only their reply trees | PARTIAL | Regression proves query shape, but large live thread pages/refresh/neighbor scenarios were not available | `artist-social-service.ts` | Post and release pagination regressions | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Follow / unfollow | Persist all profile types and sync counts/subscriptions/feed | Personal `__personal__` follows survive subscription reads; producer type is retained | PARTIAL | Full A/B follow/unfollow notification/count/refresh unavailable | follow route; `artist-social-service.ts`; type helpers | Follow-list and profile-type tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Following feed | Include followed authors only and update after unfollow | Scope isolation is tested; guest Following prompts for auth | PARTIAL | Final heterogeneous feed cursor is applied after candidate aggregation | feed scope/public-feed services | Scope tests; render-only guest prompt smoke | IMPLEMENTED; global cursor limit remains |
| Filters | Apply media/collaboration/intent/role combinations | Canonical query parsing and request state are mounted in both entry points | PARTIAL | Matching/nonmatching fixture combinations not browser-tested | feed query/client state; public feed | Query/state tests; render-only control smoke | IMPLEMENTED — FIXTURE RUNTIME NEEDED |
| Search | Search profiles/releases/publications | Canonical search is mounted and service matching includes Cyrillic and profile types | PARTIAL | Controlled result navigation not runtime-tested | global search/profile services; feed UI | Search service tests | IMPLEMENTED — FIXTURE RUNTIME NEEDED |
| Profiles | Preserve artist/group/label/producer data/actions | Producer validation/migration/labels are consistent; public profile mapping is covered | PARTIAL | Some totals are bounded snapshots; follow/message actions need A/B runtime | profile service/type/shared helpers; profile components | Profile service/type tests | IMPLEMENTED; exact historical totals remain |
| Messages | A↔B persistence, preview, unread/read, isolation | Visibility predicate fixed; only active conversation hydrates history; new unread updates read key | PARTIAL | No safe A/B/C runtime; transport remains polling | DM service/routes/panel/client state | 10 DM regressions | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Delete DM | Delete-for-me/everyone semantics and isolation | Participant-specific hiding and exact message-notification tombstoning implemented | PARTIAL | Multi-user refresh unavailable | `direct-message-service.ts`; DM routes | Delete/notification isolation tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Notifications | Exact actor/recipient/object/link/read/unread; no self-action | Self-reactions suppressed; unread uses DB count; optimistic read rollback and exact hash deletion implemented | PARTIAL | Live multi-user delivery/deep links not runtime-tested | social/notification services; topbar | Notification/social regressions | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Realtime / updates | Update without manual refresh and without full-history reload | Polling updates active state and no longer hydrates every conversation history | PARTIAL | No SSE/WebSocket push; notifications still poll | DM panel/service; topbar | DM client/service tests | Full-history defect fixed; push NOT IMPLEMENTED |
| Loading / error / not found | Stable loading, empty, missing, unavailable, deleted states | Feed request states and terminal audio unavailable state exist; auth callback is sanitized | PARTIAL | Missing/deleted post/profile/release/conversation routes need fixture browser traversal | feed client state; audio player; auth prompt | State/auth/media tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Optimistic UI | Single commit on success; rollback on failure/stale response | Stale feed responses are rejected; notification read rolls back; mutations reconcile server payloads | PARTIAL | Reaction/comment/follow/message out-of-order fault injection not run | feed/notification client state; feed UI | State/rollback tests | IMPLEMENTED — FAULT-INJECTION RUNTIME NEEDED |
| Caching / synchronization | No stale divergence across entry points after mutation | Entry points share payload/component | PARTIAL | Cross-tab and server cache invalidation after writes not runtime-proven | feed/community pages; public feed service | Route inspection and render-only smoke | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Ranking | One source for popular/trending/week using valid signals | Active routes use canonical payload; self/zero engagement and stable ties are tested | PARTIAL | Qualified-play propagation and live time-window refresh not runtime-tested | `community-engine.ts`; public feed service | Ranking unit tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Audio | Play/pause/resume/seek/range; finite missing state | Volume no longer restarts playback; failures become unavailable; range parser is covered | PARTIAL | Real S3/browser playback unavailable | feed audio player; object route; range helper | Qualified-play, range, storage tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Qualified plays | Count duration-qualified, deduped listening | Threshold `min(30s, 50%)`; seeks excluded; server identity bound; transaction lock added | PARTIAL | Two-client runtime and ranking propagation unavailable | qualified-play helper; play route/service | Qualified-play and play-service tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Feed pagination | Stable pages before expensive hydration | Payload/cursor is bounded and storage probes removed | PARTIAL | Heterogeneous candidates are still aggregated before final slice; historical post window remains bounded | dashboard/public feed services | Cursor/performance helper tests | IMPROVED; DB-native global cursor remains |
| Authorization | Enforce ownership/privacy server-side | Hidden/disabled profile mutations rejected; storage deletion owner/reference-checked; DM participant-scoped | PARTIAL | Direct adversarial A/B API runtime unavailable | social routes/services; media route; DM service | Authorization/service tests | IMPLEMENTED — RUNTIME VERIFICATION BLOCKED |
| Counters | Exact before/action/refresh totals | Deleted comment and notification unread totals corrected; play count uses server response | PARTIAL | Followers/profile all-time totals and hard-refresh actions not runtime-proven | social/profile/notification/play services | Targeted regressions | IMPLEMENTED; exact profile totals remain |
| Performance | Bound initial/next page, Community, profile, messages; record query/storage work | Initial guest `/feed` improved from 18.87 MB / 22.687 s to 305 KB / 2.314 s; storage probes removed; active DM hydration bounded | PARTIAL | Next page/profile/messages timing, DB query count, and production load not measured | dashboard/public feed service; DM service | Performance helpers plus one initial-load measurement | Initial-load defect FIXED; feature-wide audit incomplete |
| Database consistency | Detect orphan reactions/comments/follows/notifications/media without deleting real data | FK/soft-delete paths and explicit media reference checks audited; no destructive DB scan/cleanup performed | PARTIAL | Connected DB was non-fixture and later tunnel became unavailable; full orphan inventory not executed | Prisma schema/migrations; social/media services | Migration/static/service tests only | READ-ONLY AUDIT PARTIAL — DATA SCAN BLOCKED |
| Schema migrations | Canonical UUID/lowercase schema and safe repair | Four historical migrations repaired; migration tests and Prisma validate pass | PARTIAL | Three migrations unapplied in inspected env; checksum drift requires controlled deployment | four social migrations; schema | Migration SQL tests; Prisma validate | IMPLEMENTED — DEPLOYMENT VERIFICATION BLOCKED |
| Visual freeze | No layout/style/appearance changes | No CSS/theme/layout tokens were intentionally edited by the social patch, but dashboard Community now mounts the canonical feed component | PARTIAL | No before/after pixel diff exists for dashboard Community; component swap can change appearance | community page; feed component | Render-only final screenshot; no baseline diff | VISUAL REGRESSION VERIFICATION NEEDED |
| Multi-user browser QA | A/B/C write flows on isolated data | Connected DB rejected seed users and appeared non-fixture; local DB lacked initialized app schema | BLOCKED | No isolated seeded DB/S3/auth runtime | environment probes | Guest/read-only smoke only | BLOCKED BY ENVIRONMENT |

